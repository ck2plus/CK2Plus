decisions = {
	#Shattered Realm
	shattered_realm = {
		potential = {
			ai = no
			NOT = { 
				has_global_flag = reign_ancients
				has_global_flag = shattered_realm
			}
			always = no # Not ready for release
		}
		effect = {
			hidden_tooltip = {
				any_courtier = {
					limit = { is_landed = no }
					narrative_event = { id = Plus.1450 }
				}
			}
		}
	}
	reign_ancients = {
		potential = {
			ai = no
			NOT = { 
				has_global_flag = reign_ancients
				has_global_flag = shattered_realm
			}
			always = no # Not ready for release
		}
		effect = {
			hidden_tooltip = {
				any_courtier = {
					limit = { is_landed = no }
					narrative_event = { id = Plus.1451 }
				}
			}
		}
	}
	shattered_realm_test = { #will be removed once fully implemented
		potential = {
			ai = no
			always = no # Not ready for release
		}
		effect = {
			hidden_tooltip = {
				any_courtier = {
					limit = { is_landed = no }
					#narrative_event = { id = Plus.1450 } # All counts
					narrative_event = { id = Plus.1451 } # All old-religion counts
					#narrative_event = { id = Plus.1452 } # All dukes with counts
					#narrative_event = { id = Plus.1453 } # All kings with dukes
				}
			}
		}
	}	
}