decisions = {
	take_regnal_name = {
		only_playable = yes
		
		potential = {
			higher_real_tier_than = DUKE
			ai = no
			NOT = { has_character_flag = set_regnal_name }
		}
		
		allow = {
			NOT = { trait = incapable }
			prisoner = no
			custom_tooltip = {
				text = culture_allows_regnal_name
				hidden_tooltip = {
					regnal_name_culture_trigger = yes
				}
			}
			#has_nickname = no Enable this later!
		}
		
		effect = {
			hidden_tooltip = {
				set_character_flag = set_regnal_name
				select_regnal_name_effect = yes
			}
			
		}
		
		ai_will_do = {
			factor = 0
		}
	}
}

