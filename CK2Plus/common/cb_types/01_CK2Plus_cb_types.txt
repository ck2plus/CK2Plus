## normal scopes(including posttitle scopes) ##
# ROOT = actor
# FROM = recipient
# <no scope change> = thirdparty character
## title scopes ##
# ROOT = actor
# FROM = recipient
# <no scope change> = thirdparty landed title	
#
# the following effects/triggers exists (example execution order: on_success->on_success_title->on_success_posttitle):
# is_valid, is_valid_title, on_add, on_add_title, on_add_posttitle, on_success, on_success_title, on_success_posttitle, on_fail, on_fail_title, on_fail_posttitle, on_reverse_demand, on_reverse_demand_title, on_reverse_demand_posttitle
#
# Added on_attacker_leader_death, on_defender_leader_death and on_thirdparty_death, which all trigger when corresponding character dies
# These three all have war scopes, which currently has the following scope changes: 
# attacker, defender, thirdparty(only valid if thirdparty character is involved), thirdparty_title(only valid if thirdparty title is involved)
#

truce_notifier = {
	name = CB_NAME_TRUCE_NOTIFIER
	war_name = WAR_NAME_TRUCE_NOTIFIER
	sprite = 1
	truce_days = 3650
	is_permanent = yes
	#is_holy_war = yes
	can_ask_to_join_war = yes

	can_use = {
		OR = {
			ROOT = { has_character_modifier = holy_truce_timer }
			FROM = { has_character_modifier = holy_truce_timer }
		}
		ai = no
	}

	is_valid = {
		always = no
	}

	on_success = {
	}

	on_fail = {
	}

	on_reverse_demand = {
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}
}

dejure_duchy_claim = {
	name = CB_NAME_DEJURECLAIM
	war_name = WAR_NAME_DEJURECLAIM
	sprite = 16
	truce_days = 3650
	hostile_against_others = yes
	is_permanent = yes
	#is_holy_war = yes
	check_all_titles = yes # if permanent, setting this to true will check against all of someones titles, including vassal held titles
	press_claim = yes
	can_ask_to_join_war = yes

	can_use_title = {
		ROOT = {
			OR = {
				ai = no
				NOT = { has_character_modifier = do_not_disturb }
			}
		}
		ROOT = { NOT = { has_character_modifier = holy_truce_timer } }
		FROM = { NOT = { has_character_modifier = holy_truce_timer } }
		# the target is either from the same realm as the attacker 
		# or is of the same religion group and not a heresy/parent of the attacker's religion
		OR = {
			FROM = {
				religion_group = ROOT 
				NOT = {
					is_heresy_of = ROOT
					is_parent_religion = ROOT
				}
			}
			FROM = {
				same_realm = ROOT
			}
		}
		# the attacker is either muslim/reformed pagan and of the same religion at the target
		# or is neither muslim/pagan
		ROOT = {
			OR = {
				AND = {
					OR = {
						religion_group = muslim
						AND = {
							religion_group = pagan_group
							is_reformed_religion = yes
						}
					}
					religion = FROM
				}
				NOT = {
					religion_group = muslim
					religion_group = pagan_group
				}
			}	
		}
		# target title is Duke level and non-titular
		is_titular = no
		tier = duke
		# either the target has the title or their top liege is the target
		OR = {
			FROM = {
				has_landed_title = PREV
			}
			holder_scope = {
				is_liege_or_above = FROM
			}
		}
		# either a king/emperor is using it to reclaim own de jure duchy
		# or the attacker has a county in the duchy & neither the target or the attacker's liege does
		OR = {
			kingdom = {
				OR = {
					has_law = crown_authority_2
					has_law = crown_authority_3
					has_law = crown_authority_4
				}
				holder = ROOT
			}
			empire = {
				OR = {
					has_law = crown_authority_2
					has_law = crown_authority_3
					has_law = crown_authority_4
				}
				holder = ROOT
			}
			AND = {
				ROOT = {
					num_of_realm_counties = {
						value = 1
						title = PREV
					}
				}
				NOT = {
					FROM = {
						num_of_realm_counties = {
							value = 1
							title = PREV
						}
					}
					any_playable_ruler = {
						NOT = { character = ROOT }
						NOT = { character = FROM }
						NOT = { is_liege_or_above = ROOT } 
						NOT = { ROOT = { is_liege_or_above = PREV } }
						num_of_realm_counties = {
							value = 1
							title = PREV
						}
					}
				}
			}
		}
		# neither the attacker nor their top liege has the title
		NOT = {
			OR = {
				ROOT = {
					has_landed_title = PREV
				}
				holder_scope = {
					is_liege_or_above = ROOT
				}
			}
		}
		# AI will only use on AI holy orders if they're kings/emperors or of a different religion
		OR = {
			ROOT = { ai = no }
			FROM = {
				OR = {
					ai = no
					holy_order = no
					higher_tier_than = duke
					NOT = { religion = ROOT }
					NOT = { has_dlc = "Sons of Abraham" }
				}
			}
		}
	}

	is_valid_title = {
		OR = {
			FROM = {
				has_landed_title = PREV
			}
			holder_scope = {
				is_liege_or_above = FROM
			}
		}
		OR = {
			kingdom = {
				OR = {
					has_law = crown_authority_2
					has_law = crown_authority_3
					has_law = crown_authority_4
				}
				holder = ROOT
			}
			empire = {
				OR = {
					has_law = crown_authority_2
					has_law = crown_authority_3
					has_law = crown_authority_4
				}
				holder = ROOT
			}
			AND = {
				ROOT = {
					num_of_realm_counties = {
						value = 1
						title = PREV
					}
				}
				NOT = {
					FROM = {
						num_of_realm_counties = {
							value = 1
							title = PREV
						}
					}
					any_playable_ruler = {
						NOT = { character = ROOT }
						NOT = { character = FROM }
						NOT = { is_liege_or_above = ROOT } 
						NOT = { ROOT = { is_liege_or_above = PREV } }
						num_of_realm_counties = {
							value = 1
							title = PREV
						}
					}
				}
			}
		}
	}

	on_add = {
		# lose 500 piety if the target is a holy order of the same religion
		if = {
			limit = {
				FROM = {
					religion = ROOT
					holy_order = yes
				}
			}
			ROOT = { piety = -500 }
		}
	}

	on_success = {
		#change imperial decadence if the byzantine emperor
		hidden_tooltip = {
			if = {
				limit = { ROOT = { has_landed_title = e_byzantium } }
				496 = { change_variable = { which = "imperial_decadence" value = -1 } }
			}
			if = {
				limit = { FROM = { has_landed_title = e_byzantium } }
				496 = { change_variable = { which = "imperial_decadence" value = 1 } }
			}
		}
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 100
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
		FROM = {
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
	}

	on_success_title = {
		usurp_title_plus_barony_if_unlanded = { target = ROOT type = claim }
	}

	on_fail = {
		prestige = -100
	}

	on_reverse_demand = {
		#change imperial decadence if the byzantine emperor
		hidden_tooltip = {
			if = {
				limit = { FROM = { has_landed_title = e_byzantium } }
				496 = { change_variable = { which = "imperial_decadence" value = -1 } }
			}
			if = {
				limit = { ROOT = { has_landed_title = e_byzantium } }
				496 = { change_variable = { which = "imperial_decadence" value = 1 } }
			}
		}
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		ROOT = {
			prestige = -200
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 100
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}
}

vassalize = { # Subjugate
	name = CB_NAME_VASSALIZE
	war_name = WAR_NAME_VASSALIZE
	sprite = 16
	truce_days = 3650
	is_permanent = yes
	#is_holy_war = yes
	can_ask_to_join_war = yes

	can_use = {
		ROOT = {
			OR = {
				ai = no
				NOT = { has_character_modifier = do_not_disturb }
			}
		}
		ROOT = { NOT = { has_character_modifier = holy_truce_timer } }
		FROM = { NOT = { has_character_modifier = holy_truce_timer } }
		# the target is either from the same realm as the attacker 
		# or is of the same religion group and not a heresy/parent of the attacker's religion
		OR = {
			FROM = {
				religion_group = ROOT 
				NOT = {
					is_heresy_of = ROOT
					is_parent_religion = ROOT
				}
			}
			FROM = {
				same_realm = ROOT
			}
		}
		# the attacker is either muslim/reformed pagan and of the same religion at the target
		# or is neither muslim/pagan		
		OR = {
			AND = {
				OR = {
					religion_group = muslim
					AND = {
						religion_group = pagan_group
						is_reformed_religion = yes
					}
				}
				religion = FROM
			}
			NOT = {
				religion_group = muslim
				religion_group = pagan_group
			}
		}
		# target is a count or higher, dislikes the target and is disliked by them (or the attacker is a player)
		# and is both of the same religion and culture group as the attacker
		FROM = {
			higher_tier_than = baron
			religion = ROOT
			#culture_group = ROOT -- broken due to culture_group bug
			culture = ROOT
		}
		OR = {
			ROOT = { ai = no }
			FROM = {
				opinion = {
					who = ROOT
					value = -10
				}
				reverse_opinion = {
					who = ROOT
					value = -10
				}
			}
		}
		FROM = {
			# target is either independent or is of the same realm but under a different liege
			# is a lower rank than the attacker & of the same religion group
			OR = {
				independent = yes
				AND = {
					same_realm = ROOT
					NOT = { any_liege = { character = ROOT } }
				}
			}
			lower_tier_than = ROOT
			religion_group = ROOT
			# and the attacker is the de jure liege of their primary title
			# and either a count- in the attacker's duchy (or the duchy of the attacker's vassal)
			# or a duke- in the attacker's kingdom/empire (of the kingdom/empire of the attacker's vassal) with high enough CA
			primary_title = {
				de_jure_liege_or_above = ROOT
				OR = {
					AND = {
						lower_tier_than = duke
						location = {
							duchy = {
								has_holder = yes
								holder_scope = {
									OR = {
										character = ROOT
										is_liege_or_above = ROOT
									}
								}
							}
						}
					}
					AND = {
						lower_tier_than = king
						kingdom = {
							has_holder = yes
							holder_scope = {
								OR = {
									character = ROOT
									is_liege_or_above = ROOT
								}
							}
							OR = {
								has_law = crown_authority_2
								has_law = crown_authority_3
								has_law = crown_authority_4
							}
						}
					}
					AND = {
						lower_tier_than = emperor
						empire = {
							has_holder = yes
							holder_scope = {
								OR = {
									character = ROOT
									is_liege_or_above = ROOT
								}
							}
							OR = {
								has_law = crown_authority_2
								has_law = crown_authority_3
								has_law = crown_authority_4
							}
						}
					}
				}
			}
			# the target has no titles that are the attacker's de jure liege
			NOT = {
				any_demesne_title = {
					NOT = {
						AND = {
							de_jure_liege_or_above = ROOT
							OR = {
								lower_tier_than = duke
								kingdom = {
									has_holder = yes
									holder_scope = {
										OR = {
											character = ROOT
											is_liege_or_above = ROOT
										}
									}
									OR = {
										has_law = crown_authority_2
										has_law = crown_authority_3
										has_law = crown_authority_4
									}
								}
								empire = {
									has_holder = yes
									holder_scope = {
										OR = {
											character = ROOT
											is_liege_or_above = ROOT
										}
									}
									OR = {
										has_law = crown_authority_2
										has_law = crown_authority_3
										has_law = crown_authority_4
									}
								}
							}
						}
					}
				}
			}
		}
	}

	is_valid = {
		FROM = {
			OR = {
				independent = yes
				AND = {
					same_realm = ROOT
					NOT = { any_liege = { character = ROOT } }
				}
			}
			lower_tier_than = ROOT
			religion_group = ROOT
			primary_title = {
				de_jure_liege_or_above = ROOT
				OR = {
					AND = {
						lower_tier_than = duke
						location = {
							duchy = {
								has_holder = yes
								holder_scope = {
									OR = {
										character = ROOT
										is_liege_or_above = ROOT
									}
								}
							}
						}
					}
					AND = {
						lower_tier_than = king
						kingdom = {
							has_holder = yes
							holder_scope = {
								OR = {
									character = ROOT
									is_liege_or_above = ROOT
								}
							}
							OR = {
								has_law = crown_authority_2
								has_law = crown_authority_3
								has_law = crown_authority_4
							}
						}
					}
					AND = {
						lower_tier_than = emperor
						empire = {
							has_holder = yes
							holder_scope = {
								OR = {
									character = ROOT
									is_liege_or_above = ROOT
								}
							}
							OR = {
								has_law = crown_authority_2
								has_law = crown_authority_3
								has_law = crown_authority_4
							}
						}
					}
				}
			}
			NOT = {
				any_demesne_title = {
					NOT = {
						AND = {
							de_jure_liege_or_above = ROOT
							OR = {
								lower_tier_than = duke
								kingdom = {
									has_holder = yes
									holder_scope = {
										OR = {
											character = ROOT
											is_liege_or_above = ROOT
										}
									}
									OR = {
										has_law = crown_authority_2
										has_law = crown_authority_3
										has_law = crown_authority_4
									}
								}
								empire = {
									has_holder = yes
									holder_scope = {
										OR = {
											character = ROOT
											is_liege_or_above = ROOT
										}
									}
									OR = {
										has_law = crown_authority_2
										has_law = crown_authority_3
										has_law = crown_authority_4
									}
								}
							}
						}
					}
				}
			}
		}
	}

	on_success = {
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 100
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
		FROM = {
			hidden_tooltip = {
				opinion = {
					who = ROOT
					modifier = force_vassalized
					months = 240
				}
			}
			set_defacto_liege = ROOT
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
	}

	on_fail = {
		ROOT = {
			prestige = -100
		}
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		ROOT = {
			prestige = -200
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 100
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}
}

restore_papacy = {
	name = CB_NAME_RESTORE_PAPACY
	war_name = WAR_NAME_RESTORE_PAPACY
	sprite = 10
	truce_days = 3650
	is_permanent = yes
	#is_holy_war = yes
	can_ask_to_join_war = yes

	can_use = {
		ROOT = {
			OR = {
				ai = no
				NOT = { has_character_modifier = do_not_disturb }
			}
		}
		ROOT = { NOT = { has_character_modifier = holy_truce_timer } }
		FROM = { NOT = { has_character_modifier = holy_truce_timer } }
		# the target is not part of the attacker's realm
		NOT = {
			FROM = {
				same_realm = ROOT
			}
		}
		# the target is not already involved in a restore papacy war
		NOT = {
			FROM = {
				any_war = {
					using_cb = restore_papacy
				}
			}
		}
		# the AI will not attempt to use this CB a second time
		NOT = {
			ROOT = {
				ai = yes
				has_character_flag = restore_papacy_attempted
			}
		}
		# the attacker is catholic & catholicism has a RA of 20%+
		ROOT = {
			religion = catholic
			religion_authority = 0.2
		}
		FROM = {
			# the target is not the pope
			NOT = { has_landed_title = k_papal_state }
			# the target is either the pope's liege or owns Rome or has a realm lord who owns Rome
			OR = {
				k_papal_state = { holder_scope = { any_liege = { character = FROM } } }
				has_landed_title = c_roma
				any_realm_lord = {
					has_landed_title = c_roma
				}
			}
			# the pope is either independent or is a vassal of the target or the attacker
			OR = {
				k_papal_state = { holder_scope = { independent = yes } }
				k_papal_state = { holder_scope = { any_liege = { character = FROM } } }
				k_papal_state = { holder_scope = { any_liege = { character = ROOT } } }
			}
		}
	}

	on_add = {
		ROOT = {
			set_character_flag = restore_papacy_attempted
		}
	}

	on_success = {
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		# the Pope regains Rome if owned by the target's realm and becomes independent if vassalized
		FROM = {
			if = {
				limit = {
					OR = {
						has_landed_title = c_roma
						any_realm_lord = {
							has_landed_title = c_roma
						}
					}
				}
				k_papal_state = {
					holder_scope = {
						c_roma = {
							usurp_title_plus_barony_if_unlanded = { target = PREV type = claim }
						}
					}
				}
			}
			if = {
				limit = {
					k_papal_state = { holder_scope = { any_liege = { character = FROM } } }
				}
				k_papal_state = { holder_scope = { set_defacto_liege = THIS } }
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 100
			participation_scaled_piety = 100
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 100 participation_scaled_piety = 100 }
		}
		FROM = {
			prestige = -200
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
	}

	on_fail = {
		ROOT = {
			prestige = -100
			piety = -100
		}
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		ROOT = {
			prestige = -200
			piety = -200
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 200
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 200 }
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}

	ai_will_do = { 
		factor = 2
		modifier = {
			factor = 5
			ROOT = { trait = zealous }
		}
		modifier = {
			factor = 0
			ROOT = { trait = cynical }
		}
	}
}

restore_byzantium = { #Restore Byzantium
	name = CB_NAME_RESTOREBYZANTIUM
	war_name = WAR_NAME_RESTOREBYZANTIUM
	sprite = 16
	truce_days = 3650
	is_permanent = yes
	#is_holy_war = yes
	check_de_jure_tier = EMPEROR
	can_ask_to_join_war = yes

	can_use_title = {
		ROOT = {
			OR = {
				ai = no
				NOT = { has_character_modifier = do_not_disturb }
			}
		}
		ROOT = { NOT = { has_character_modifier = holy_truce_timer } }
		FROM = { NOT = { has_character_modifier = holy_truce_timer } }
		# the Byzantine Empire does not exist
		e_byzantium = { has_holder = no }
		# the attacker is an independent Greek Orthodox (or a heretic) king or emperor
		ROOT = {
			OR = {
				religion = orthodox
				religion = bogomilist
				religion = nestorian
				religion = monothelite
				religion = iconoclast
			}
			culture_group = byzantine
			independent = yes
			higher_tier_than = duke
		}
		# the defender is either not Orthodox (or a heretic) or not Greek
		FROM = {
			OR = {
				NOT = { 
					religion = orthodox
					religion = bogomilist
					religion = nestorian
					religion = monothelite
					religion = iconoclast
				}
				NOT = { culture_group = byzantine }
			}
		}
		title = e_byzantium
		has_holder = no
		# byzantion's holder is in the target's realm
		c_byzantion = { holder_scope = { same_realm = FROM } }
		NOT = {
			OR = {
				ROOT = {
					has_landed_title = PREV
				}
				holder_scope = {
					is_liege_or_above = ROOT
				}
			}
		}
	}

	is_valid_title = {
		ROOT = {
			OR = {
				religion = orthodox
				religion = bogomilist
				religion = nestorian
				religion = monothelite
				religion = iconoclast
			}
			culture_group = byzantine
			independent = yes
		}

		FROM = {
			OR = {
				NOT = {
					religion = orthodox
					religion = bogomilist
					religion = nestorian
					religion = monothelite
					religion = iconoclast
				}
				NOT = { culture_group = byzantine }
			}
		}

		title = e_byzantium
		has_holder = no
		c_byzantion = { holder_scope = { same_realm = FROM } }

		NOT = {
			OR = {
				ROOT = {
					has_landed_title = PREV
				}
				holder_scope = {
					is_liege_or_above = ROOT
				}
			}
		}
	}

	on_success = {
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 200
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 200 }
		}
		FROM = {
			prestige = -200
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
	}

	on_success_title = {
		hidden_tooltip = {
			any_de_jure_vassal_title = { # take all baronies under the one we're fighting for
				limit = {
					has_holder = yes
					holder_scope = {
						OR = {
							character = FROM
							is_liege_or_above = FROM
						}
					}
				}

				usurp_title_plus_barony_if_unlanded = { target = ROOT type = claim }
			}
		}
		tooltip = { e_byzantium = { gain_title = ROOT } }
		ROOT = { set_character_flag = centralize_without_penalties }
		hidden_tooltip = {
			if = {
				limit = {
					c_byzantion = { holder_scope = { same_realm = ROOT } }
				}
				e_byzantium = {
					revoke_law = investiture_law_0
					revoke_law = investiture_law_1
					add_law = crown_authority_1
					add_law = city_tax_0
					add_law = temple_tax_0
					add_law = tax_privileges_1
					add_law = levy_privileges_1
					add_law = council_privileges_0
					add_law = feudal_tax_0
					add_law = succ_primogeniture
					add_law = cognatic_succession
					gain_title = ROOT
				}
			}
		}
		ROOT = { clr_character_flag = centralize_without_penalties }
		#shatter empire if it's too small
		hidden_tooltip = { FROM = { character_event = { id = Plus.303 } } }
	}

	on_fail = {
		prestige = -100
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		ROOT = {
			prestige = -200
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 200
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 200 }
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}
}

free_family_cb = { # Can't be used to save close family due to hard coded limit
	name = CB_NAME_FREE_FAMILY
	war_name = WAR_NAME_FREE_FAMILY
	sprite = 1
	truce_days = 3650
	is_permanent = yes
	hostile_against_others = no
	can_call_allies = yes # Can either side call allies
	can_attack_vassals = no
	
	can_use = {
		ROOT = {
			OR = {
				ai = no
				NOT = { has_character_modifier = do_not_disturb }
			}
		}
		ROOT = { NOT = { has_character_modifier = holy_truce_timer } }
		FROM = { NOT = { has_character_modifier = holy_truce_timer } }
		# one of the attacker's dynasty members is a prisoner
		ROOT = {
			any_dynasty_member = {
				prisoner = yes
			}
		}		
		
	}
	
	can_use_title = {
		# the target has a prisoner of the attacker's dynasty
		FROM = {
			any_courtier = {
				prisoner = yes
				dynasty = ROOT
			}
		}
	}
	
	is_valid_title = {
		FROM = {
			any_courtier = {
				prisoner = yes
				dynasty = ROOT
			}
		}
	}	
	
	on_success = {
		ROOT = { 
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
			# free all members of the attacker's dynasty
			any_dynasty_member = {
				limit = {
					prisoner = yes
					host = { character = FROM }
				}
				prisoner = no
			}
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}		
		FROM = {
			prestige = -200
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}		
	}
	
	on_fail = {
		ROOT = {
			prestige = -100
		}
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		ROOT = {
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
			# kill all prisoners belonging to the attacker's dynasty
			any_dynasty_member = {
				limit = {
					prisoner = yes
					host = { character = FROM }
				}
				death = {
					death_reason = death_execution
					killer = FROM
				}
			}
			any_defender = {
				limit = { character = FROM }
				participation_scaled_prestige = 200
			}
			any_defender = {
				limit = { NOT = { character = FROM } }
				hidden_tooltip = { participation_scaled_prestige = 200 }
			}
		}
	}
	
	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	defender_ai_defeat_worth = {
		factor = 100
	}
}

coastal_city_republic = { #Take a coastal city (Barony Tier)
	name = CB_NAME_COASTAL_COUNTY_REPUBLIC
	war_name = WAR_NAME_COASTAL_CITY_REPUBLIC
	sprite = 1
	truce_days = 3650
	is_permanent = yes
	check_all_titles = yes
	can_ask_to_join_war = yes
	
	allow_distant = yes # AI parameter
	
	can_use = {
		ROOT = {
			OR = {
				is_merchant_republic = yes
				is_patrician = yes
			}
			NOT = { 
				is_liege_or_above = FROM
				is_vassal_or_below = FROM 
				same_liege = FROM
				has_character_modifier = rep_cb_cd
			}
			prisoner = no
			has_regent = no
		}
	}
	
	can_use_title = {
		location = {
			port = yes
			OR = {
				# Within Free CB range
				ROOT = {
					capital_scope = {
						location = {
							NOT = {
								distance = {
									where = PREVPREVPREV # Comparing the distance between ROOT's capital and the title in question
									value = 590	#  From  aprox.Venice to East Cyprus and from Venice to Gibraltar
								}
							}
						}	
					}
				}
				AND = { # Has a trade post here
					OR = {
						PREV = {
							holder_scope = {
								NOT = { same_realm = ROOT }
								OR = {
									is_patrician = yes
									is_merchant_republic = yes
								}
							}
						}
						trade_post_owner = {
							OR = {
								character = ROOT
								is_liege_or_above = ROOT
							}
						}
					}				
				}
			}
			NOT = { # Prevent the AI from overextending itself, it won't understand the mechanic anyways
				ROOT = {
					ai = yes
					capital_scope = {
						location = {
							distance = {
								where = PREVPREVPREV # Comparing the distance between ROOT's capital and the title in question
								value = 590	#  From  aprox.Venice to East Cyprus and from Venice to Gibraltar
							}
						}	
					}
				}
			}
		}
		tier = BARON
		is_republic = yes # Cities only
		NOT = { owner = { character = FROM } } # Can't take the primary holding away from someone	
		
		ROOT = {
			OR = {
				ai = no
				NOT = { has_character_modifier = do_not_disturb }
			}
		}
		ROOT = { NOT = { has_character_modifier = holy_truce_timer } }
		FROM = { NOT = { has_character_modifier = holy_truce_timer } }		
	}	

	is_valid = {
		ROOT = {
			OR = {
				is_merchant_republic = yes
				is_patrician = yes
			}
		}
	}

	is_valid_title = {
		NOT = { owner = { character = FROM } } # Can't take the primary holding away from someone
	}
	
	on_add = {
		ROOT = {
			add_character_modifier = {
				name = rep_cb_cd
				duration = 1825
				hidden = no
				inherit = yes
			}
		}
	}

	on_success = {
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		FROM = {
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
	}
	
	on_success_title = {
		usurp_title = ROOT
		
		if = {
			limit = {
				location = {
					port = yes
					ROOT = {
						capital_scope = {
							location = {
								NOT = {
									distance = {
										where = PREVPREVPREV # Comparing the distance between ROOT's capital and the title in question
										value = 590	#  From  aprox.Venice to East Cyprus and from Venice to Gibraltar
									}
								}
							}	
						}
					}			
				}
			}
			custom_tooltip = {
				text = CTT_REP_IN_RANGE
			}
		}
		if = {
			limit = {
				location = {
					port = yes
					ROOT = {
						capital_scope = {
							location = {
								distance = {
									where = PREVPREVPREV # Comparing the distance between ROOT's capital and the title in question
									value = 590	#  From  aprox.Venice to East Cyprus and from Venice to Gibraltar
								}
							}	
						}
					}			
				}			
			}
			custom_tooltip = {
				text = CTT_REP_OUT_RANGE
			}			
		}		
		
		hidden_tooltip = {
			ROOT = {
				remove_character_modifier = rep_cb_cd
				add_character_modifier = {
					name = rep_cb_cd
					duration = 1825
					hidden = no
					inherit = yes
				}
			}
		}
		
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 100
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
	}

	on_fail = {
		ROOT = {
			prestige = -100
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 50
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 50 }
		}
		
		hidden_tooltip = {
			ROOT = { remove_character_modifier = rep_cb_cd }
		}
	}

	on_reverse_demand = {
		ROOT = {
			prestige = -200
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 100
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
		hidden_tooltip = {
			ROOT = { remove_character_modifier = rep_cb_cd }
		}
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	defender_ai_defeat_worth = {
		factor = 100
	}
}

claim_on_liege_plot = {
	name = CB_NAME_CLAIM
	war_name = WAR_NAME_PRETENDER
	sprite = 3
	truce_days = 3650
	hostile_against_others = yes

	can_call_allies = no
	can_ask_to_join_war = no
	major_revolt = yes
	
	can_use_title = {
		FROM = {
			has_landed_title = PREV
		}
	}

	is_valid_title = {
		FROM = {
			has_landed_title = PREV
		}
	}

	on_success = {
		# weakly-held vassals may choose to declare independence
		if = {
			limit = {
				FROM = { independent = yes }
			}
			hidden_tooltip = {
				FROM = { 
					any_vassal = {
						character_event = { id = Plus.430 days = 3 }
					}
				}
			}
		}
		# all attacker allies become vassals and all loyalists lose the trait & are imprisoned by the attacker
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				reverse_opinion = { who = ROOT modifier = opinion_civil_war_supporter months = 60 }
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
				}
			}
			any_defender = {
				limit = { trait = loyalist }
				remove_trait = loyalist
				imprison = ROOT
				reverse_opinion = { who = ROOT modifier = opinion_civil_war_loser months = 60 }
				reverse_remove_opinion = { who = liege modifier = supported_civil_war }
			}
		}
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
		}
		FROM = { imprison = ROOT }
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 100
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}	
		FROM = {
			prestige = -500
			clr_character_flag = faction_pretender_war
		}
	}
	
	on_success_title = {
		if = {
			limit = {
				is_primary_holder_title = yes
			}
			FROM = {
				any_demesne_title = {
					limit =	{
						is_primary_holder_title_tier = yes
						OR = {
							tier = king
							tier = emperor
						}
					}
					if = {
						limit = { has_law = crown_authority_1 }
						add_law_w_cooldown = crown_authority_0
					}
					if = {
						limit = { has_law = crown_authority_2 }
						add_law_w_cooldown = crown_authority_1
					}
					if = {
						limit = { has_law = crown_authority_3 }
						add_law_w_cooldown = crown_authority_2
					}
					if = {
						limit = { has_law = crown_authority_4 }
						add_law_w_cooldown = crown_authority_3
					}									
				}
				any_demesne_title = { # Decentralize all titles
					limit = {
						OR = {
							has_law = tribal_organization_1
							has_law = tribal_organization_2
							has_law = tribal_organization_3
							has_law = tribal_organization_4
						}
					}
					if = {
						limit = { has_law = tribal_organization_1 }
						add_law = tribal_organization_0
					}
					if = {
						limit = { has_law = tribal_organization_2 }
						add_law = tribal_organization_1
					}
					if = {
						limit = { has_law = tribal_organization_3 }
						add_law = tribal_organization_2
					}
					if = {
						limit = { has_law = tribal_organization_4 }
						add_law = tribal_organization_3
					}
				}
				any_demesne_title = {
					limit = {
						is_primary_holder_title_tier = yes
						is_primary_holder_title = no
					}
					usurp_title_plus_barony_if_unlanded = { target = ROOT type = claim }
				}
				primary_title = {
					usurp_title_plus_barony_if_unlanded = { target = ROOT type = claim }
					hidden_tooltip = { make_primary_title = yes }
				}
				set_defacto_liege = ROOT		
			}
			hidden_tooltip = {
				if = {
					limit = {
						FROM = {
							ROOT = { 
								primary_title = { 
									NOT = { has_law = succ_feudal_elective } 
									FROM = {
										capital_scope = {
											de_jure_liege_or_above = PREVPREV
										}
									}
								} 
							}
							OR = {
								num_of_count_titles = 2
								primary_title = { higher_tier_than = count }
							}
						}
					}
					FROM = {
						capital_scope = {
							county = {
								usurp_title_plus_barony_if_unlanded = { target = ROOT type = claim }
							}
						}
					}			
				}	
			}
			hidden_tooltip = {
				FROM = { 
					set_defacto_liege = ROOT 
					any_vassal = {
						limit = {
							NOT = { primary_title = { de_jure_liege_or_above = PREVPREV } }
						}
						set_defacto_liege = ROOT 
					}
				}
			}			
		}
		if = {
			limit = {
				is_primary_holder_title = no
				holder = FROM
				OR = {
					tier = king
					tier = emperor
				}
			}
			if = {
				limit = { has_law = crown_authority_1 }
				add_law_w_cooldown = crown_authority_0
			}
			if = {
				limit = { has_law = crown_authority_2 }
				add_law_w_cooldown = crown_authority_1
			}
			if = {
				limit = { has_law = crown_authority_3 }
				add_law_w_cooldown = crown_authority_2
			}
			if = {
				limit = { has_law = crown_authority_4 }
				add_law_w_cooldown = crown_authority_3
			}
		}
		if = {
			limit = {
				is_primary_holder_title = no
				holder = FROM
			}
			if = {
				limit = { has_law = tribal_organization_1 }
				add_law = tribal_organization_0
			}
			if = {
				limit = { has_law = tribal_organization_2 }
				add_law = tribal_organization_1
			}
			if = {
				limit = { has_law = tribal_organization_3 }
				add_law = tribal_organization_2
			}
			if = {
				limit = { has_law = tribal_organization_4 }
				add_law = tribal_organization_3
			}
		}
		if = {
			limit = {
				is_primary_holder_title = no
				holder = FROM
			}
			usurp_title_plus_barony_if_unlanded = { target = ROOT type = claim }
		}
		hidden_tooltip = {		
			# post-civil war event to reward allies
			ROOT = { character_event = { id = PlusFaction.450 } }
		}
		# recalculate contiguous territory province flags
		hidden_tooltip = { k_papal_state = { holder_scope = { character_event = { id = Plus.307 } } } }
		# change faction moods
		FROM = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		ROOT = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
	}

	on_fail = {
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				remove_trait = rebel
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
				}
			}
			FROM = {
				any_vassal = {
					limit = { trait = loyalist }
					remove_trait = loyalist
					reverse_remove_opinion = { who = PREV modifier = supported_civil_war }
				}
				any_vassal = {
					limit = { trait = neutral }
					remove_trait = neutral
				}
			}
		}	
		ROOT = {
			prestige = -100
		}
		FROM = {
			clr_character_flag = faction_pretender_war
		}
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
		}
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				imprison = FROM
				remove_trait = rebel
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
					opinion = { who = PREV modifier = opinion_civil_war_loser months = 60 }
					reverse_opinion = { who = PREV modifier = opinion_coerced_into_leaving_faction months = 60 }
				}
			}		
		}		
		ROOT = {
			prestige = -200
			tooltip = { imprison = FROM }
		}
		hidden_tooltip = {
			FROM = {
				any_vassal = {
					limit = { trait = loyalist }
					reverse_opinion = { who = PREV modifier = opinion_civil_war_supporter months = 60 }
					reverse_remove_opinion = { who = PREV modifier = supported_civil_war }
				}
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 100
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
		hidden_tooltip = {
			# post-civil war event to reward allies
			FROM = { character_event = { id = PlusFaction.450 } } 
		}
		FROM = {
			clr_character_flag = faction_pretender_war
		}
	}
	
	on_reverse_demand_title = {
		ROOT = {	
			any_claim = {
				limit = { holder_scope = { character = FROM } }
				remove_claim = ROOT
			}
		}
	}
	
	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	defender_ai_defeat_worth = {
		factor = 100
	}
}

other_claim_on_liege_plot = {
	name = CB_NAME_CLAIM
	war_name = WAR_NAME_OTHER_PRETENDER
	sprite = 3
	truce_days = 3650
	hostile_against_others = yes

	can_call_allies = no
	can_ask_to_join_war = no
	major_revolt = yes
	
	can_use_title = {
		FROM = {
			has_landed_title = PREV
		}
	}

	is_valid_title = {
		FROM = {
			has_landed_title = PREV
		}
	}

	on_success = {
		# any weakly-held vassals may choose to declare independence
		if = {
			limit = {
				FROM = { independent = yes }
			}
			hidden_tooltip = {
				FROM = { 
					any_vassal = {
						character_event = { id = Plus.430 days = 3 }
					}
				}
			}
		}
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				reverse_opinion = { who = ROOT modifier = opinion_civil_war_supporter months = 60 }
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
				}
			}
			any_defender = {
				limit = { trait = loyalist }
				remove_trait = loyalist
				imprison = ROOT
				reverse_opinion = { who = ROOT modifier = opinion_civil_war_loser months = 60 }
				reverse_remove_opinion = { who = liege modifier = supported_civil_war }
			}			
		}
		FROMFROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
		}
		ROOT = {
			opinion = {
				modifier = pressed_my_claim 
				who = PREV
			}
		}
		FROM = { imprison = ROOT }
		any_attacker = {
			limit = { character = FROMFROM }
			participation_scaled_prestige = 100
		}
		any_attacker = {
			limit = { NOT = { character = FROMFROM } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
		FROM = {
			prestige = -500
			clr_character_flag = faction_pretender_war
		}
	}
	
	on_success_title = {
		if = {
			limit = {
				is_primary_holder_title = yes
			}
			FROM = {
				any_demesne_title = {
					limit =	{
						is_primary_holder_title_tier = yes
						OR = {
							tier = king
							tier = emperor
						}
					}
					if = {
						limit = { has_law = crown_authority_1 }
						add_law_w_cooldown = crown_authority_0
					}
					if = {
						limit = { has_law = crown_authority_2 }
						add_law_w_cooldown = crown_authority_1
					}
					if = {
						limit = { has_law = crown_authority_3 }
						add_law_w_cooldown = crown_authority_2
					}
					if = {
						limit = { has_law = crown_authority_4 }
						add_law_w_cooldown = crown_authority_3
					}									
				}
				any_demesne_title = { # Decentralize all titles
					limit = {
						OR = {
							has_law = tribal_organization_1
							has_law = tribal_organization_2
							has_law = tribal_organization_3
							has_law = tribal_organization_4
						}
					}
					if = {
						limit = { has_law = tribal_organization_1 }
						add_law = tribal_organization_0
					}
					if = {
						limit = { has_law = tribal_organization_2 }
						add_law = tribal_organization_1
					}
					if = {
						limit = { has_law = tribal_organization_3 }
						add_law = tribal_organization_2
					}
					if = {
						limit = { has_law = tribal_organization_4 }
						add_law = tribal_organization_3
					}
				}
				any_demesne_title = {
					limit = {
						is_primary_holder_title_tier = yes
						is_primary_holder_title = no
					}
					usurp_title_plus_barony_if_unlanded = { target = ROOT type = claim }
				}
				primary_title = {
					usurp_title_plus_barony_if_unlanded = { target = ROOT type = claim }
					hidden_tooltip = { make_primary_title = yes }
				}
				set_defacto_liege = ROOT		
			}
			hidden_tooltip = {
				if = {
					limit = {
						FROM = {
							ROOT = { 
								primary_title = { 
									NOT = { has_law = succ_feudal_elective } 
									FROM = {
										capital_scope = {
											de_jure_liege_or_above = PREVPREV
										}
									}
								} 
							}
							OR = {
								num_of_count_titles = 2
								primary_title = { higher_tier_than = count }
							}
						}
					}
					FROM = {
						capital_scope = {
							county = {
								usurp_title_plus_barony_if_unlanded = { target = ROOT type = claim }
							}
						}
					}			
				}	
			}
			hidden_tooltip = {
				FROM = { 
					set_defacto_liege = ROOT 
					any_vassal = {
						limit = {
							NOT = { primary_title = { de_jure_liege_or_above = PREVPREV } }
						}
						set_defacto_liege = ROOT 
					}
				}
			}			
		}
		if = {
			limit = {
				is_primary_holder_title = no
				holder = FROM
				OR = {
					tier = king
					tier = emperor
				}
			}
			if = {
				limit = { has_law = crown_authority_1 }
				add_law_w_cooldown = crown_authority_0
			}
			if = {
				limit = { has_law = crown_authority_2 }
				add_law_w_cooldown = crown_authority_1
			}
			if = {
				limit = { has_law = crown_authority_3 }
				add_law_w_cooldown = crown_authority_2
			}
			if = {
				limit = { has_law = crown_authority_4 }
				add_law_w_cooldown = crown_authority_3
			}
		}
		if = {
			limit = {
				is_primary_holder_title = no
				holder = FROM
			}
			if = {
				limit = { has_law = tribal_organization_1 }
				add_law = tribal_organization_0
			}
			if = {
				limit = { has_law = tribal_organization_2 }
				add_law = tribal_organization_1
			}
			if = {
				limit = { has_law = tribal_organization_3 }
				add_law = tribal_organization_2
			}
			if = {
				limit = { has_law = tribal_organization_4 }
				add_law = tribal_organization_3
			}
		}
		if = {
			limit = {
				is_primary_holder_title = no
				holder = FROM
			}
			usurp_title_plus_barony_if_unlanded = { target = ROOT type = claim }					
		}
		hidden_tooltip = {		
			# post-civil war event to reward allies
			ROOT = { character_event = { id = PlusFaction.450 } }
		}
		# recalculate contiguous territory province flags
		hidden_tooltip = { k_papal_state = { holder_scope = { character_event = { id = Plus.307 } } } }
		# change faction moods
		FROM = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		ROOT = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
	}

	on_fail = {
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				remove_trait = rebel
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
				}
			}
			FROM = {
				any_vassal = {
					limit = { trait = loyalist }
					remove_trait = loyalist
					reverse_remove_opinion = { who = PREV modifier = supported_civil_war }
				}
				any_vassal = {
					limit = { trait = neutral }
					remove_trait = neutral
				}
			}
		}
		FROMFROM = {
			prestige = -100
		}
		FROM = {
			clr_character_flag = faction_pretender_war
		}
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
		}
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				imprison = FROM
				remove_trait = rebel
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
					opinion = { who = PREV modifier = opinion_civil_war_loser months = 60 }
					reverse_opinion = { who = PREV modifier = opinion_coerced_into_leaving_faction months = 60 }
				}
			}		
		}		
		FROMFROM = {
			prestige = -200
		}
		if = {	
			limit = {
				ROOT = {
					any_liege = {
						character = FROM
						NOT = { has_opinion_modifier = { who = PREV modifier = opinion_civil_war_loser } }
					}
				}
			}
			ROOT = {
				imprison = FROM
				hidden_tooltip = {
					reverse_opinion = { who = FROM modifier = opinion_civil_war_loser months = 60 }	
				}
			}
		}
		hidden_tooltip = {
			FROM = {
				any_vassal = {
					limit = { trait = loyalist }
					reverse_opinion = { who = PREV modifier = opinion_civil_war_supporter months = 60 }
					reverse_remove_opinion = { who = PREV modifier = supported_civil_war }
				}
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 100
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
		hidden_tooltip = {
			# post-civil war event to reward allies
			FROM = { character_event = { id = PlusFaction.450 } }
			FROM = { clr_character_flag = faction_pretender_war }
		}
		# change faction moods
		FROM = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		ROOT = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
	}
	
	on_reverse_demand_title = {
		ROOT = {	
			any_claim = {
				limit = { holder_scope = { character = FROM } }
				remove_claim = ROOT
			}
		}
	}
	
	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	defender_ai_defeat_worth = {
		factor = 100
	}
}

cb_faction_overthrow_ruler = {
	name = CB_NAME_OVERTHROWRULER
	war_name = WAR_NAME_FACTION_DEPOSE
	sprite = 12
	truce_days = 3650
	
	is_revolt_cb = yes
	major_revolt = yes
	can_call_allies = no
	can_ask_to_join_war = no
	
	can_use = {
		ROOT = {
			vassal_of = FROM
		}
	}

	is_valid = {
		ROOT = {
			OR = {
				liege = {
					character = PREV # either independent
				}
				liege = { 
					FROM = { 
						is_liege_or_above = PREV # or have shared liege
					}
				}
			}
		}
		FROM = {
			any_demesne_title = {
				NOT = { lower_tier_than = ROOT }
				temporary = no
			}
		}
	}

	on_success = {
		hidden_tooltip = { 
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_character_flag = rebel_attacker
				opinion = { modifier = overthrown_ruler who = FROM }
				reverse_opinion = { modifier = overthrew_me who = FROM }
			}
		}
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
		}
		# weakly-held vassals of the defender may declare independence
		if = {
			limit = {
				FROM = { independent = yes }
			}
			hidden_tooltip = {
				FROM = {
					any_vassal = {
						character_event = { id = Plus.430 days = 3 }
					}
				}
			}
		}
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				remove_trait = rebel
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
				}
			}
		}
		FROM = {
			prestige = -500
			hidden_tooltip = {
				remove_opinion = { who = ROOT modifier = declared_war }
				any_vassal = {
					limit = { trait = loyalist }
					remove_trait = loyalist
					reverse_remove_opinion = { who = PREV modifier = supported_civil_war }
				}
				any_vassal = {
					limit = { trait = neutral }
					remove_trait = neutral
				}
			}
			any_demesne_title = { # Decentralize all titles
				limit = {
					OR = {
						tier = king
						tier = emperor
					}
					OR = {
						has_law = crown_authority_1
						has_law = crown_authority_2
						has_law = crown_authority_3
						has_law = crown_authority_4
					}
				}
				if = {
					limit = { has_law = crown_authority_1 }
					add_law = crown_authority_0
				}
				if = {
					limit = { has_law = crown_authority_2 }
					add_law = crown_authority_1
				}
				if = {
					limit = { has_law = crown_authority_3 }
					add_law = crown_authority_2
				}
				if = {
					limit = { has_law = crown_authority_4 }
					add_law = crown_authority_3
				}
			}
			any_demesne_title = { # Decentralize all titles
				limit = {
					OR = {
						has_law = tribal_organization_1
						has_law = tribal_organization_2
						has_law = tribal_organization_3
						has_law = tribal_organization_4
					}
				}
				if = {
					limit = { has_law = tribal_organization_1 }
					add_law = tribal_organization_0
				}
				if = {
					limit = { has_law = tribal_organization_2 }
					add_law = tribal_organization_1
				}
				if = {
					limit = { has_law = tribal_organization_3 }
					add_law = tribal_organization_2
				}
				if = {
					limit = { has_law = tribal_organization_4 }
					add_law = tribal_organization_3
				}
			}

			# enforce any rebel demands that prompted the civil war
			if = {
				limit = { ROOT = { has_character_flag = demands_tax_privilege } }
				primary_title = {
					if = {
						limit = { has_law = tax_privileges_1 }
						add_law_w_cooldown = tax_privileges_2
					}
					if = {
						limit = { has_law = tax_privileges_0 }
						add_law_w_cooldown = tax_privileges_1
					}
				}
			}
			if = {
				limit = { ROOT = { has_character_flag = demands_levy_privilege } }
				primary_title = {
					add_law_w_cooldown = levy_privileges_1
				}
			}
			if = {
				limit = { ROOT = { has_character_flag = demands_lower_feudal_tax } }
				primary_title = {
					if = {
						limit = { has_law = feudal_tax_1 }
						add_law_w_cooldown = feudal_tax_0
					}
					if = {
						limit = { has_law = feudal_tax_2 }
						add_law_w_cooldown = feudal_tax_1
					}
					if = {
						limit = { has_law = iqta_tax_1 }
						add_law_w_cooldown = iqta_tax_0
					}
					if = {
						limit = { has_law = iqta_tax_2 }
						add_law_w_cooldown = iqta_tax_1
					}
				}
			}
			if = {
				limit = { ROOT = { has_character_flag = demands_lower_city_tax } }
				primary_title = {
					if = {
						limit = { has_law = city_tax_1 }
						add_law_w_cooldown = city_tax_0
					}
					if = {
						limit = { has_law = city_tax_2 }
						add_law_w_cooldown = city_tax_1
					}
				}
			}
			if = {
				limit = { ROOT = { has_character_flag = demands_lower_temple_tax } }
				primary_title = {
					if = {
						limit = { has_law = temple_tax_1 }
						add_law_w_cooldown = temple_tax_0
					}
					if = {
						limit = { has_law = temple_tax_2 }
						add_law_w_cooldown = temple_tax_1
					}
				}
			}
			if = {
				limit = { ROOT = { has_character_flag = demands_revert_succession } }
				primary_title = {
					if = {
						limit = { has_title_flag = former_gavelkind }
						clr_title_flag = former_gavelkind
						add_law = succ_gavelkind
					}
					if = {
						limit = { has_title_flag = former_seniority }
						clr_title_flag = former_seniority
						add_law = succ_seniority
					}
					if = {
						limit = { has_title_flag = former_primogeniture }
						clr_title_flag = former_primogeniture
						add_law = succ_primogeniture
					}
					if = {
						limit = { has_title_flag = former_feudal_elective }
						clr_title_flag = former_feudal_elective
						add_law = succ_feudal_elective
					}
					if = {
						limit = { has_title_flag = former_tanistry }
						clr_title_flag = former_tanistry
						add_law = succ_tanistry
					}
					if = {
						limit = { has_title_flag = former_ultimogeniture }
						clr_title_flag = former_ultimogeniture
						add_law = succ_ultimogeniture
					}
					if = {
						limit = { has_title_flag = former_cognatic_succession }
						clr_title_flag = former_cognatic_succession
						add_law = cognatic_succession
					}
					if = {
						limit = { has_title_flag = former_agnatic_succession }
						clr_title_flag = former_agnatic_succession
						add_law = agnatic_succession
					}
					if = {
						limit = { has_title_flag = former_true_cognatic_succession }
						clr_title_flag = former_true_cognatic_succession
						add_law = true_cognatic_succession
					}
				}
			}
			if = {
				limit = { ROOT = { has_character_flag = demands_council_life_terms } }
				primary_title = {
					add_law_w_cooldown = council_privileges_1
				}
			}

			# pass on any counties to claimant/de jure rebels
			if = {
				limit = { num_of_count_titles = 2 }
				any_demesne_title = {
					limit = {
						can_be_given_away = yes
						tier = count
						is_feudal = yes
						location = { is_capital = no }
					}
					if = {
						limit = {
							any_playable_ruler = {
								has_character_flag = rebel_attacker
								any_demesne_title = {
									any_direct_de_jure_vassal_title = { title = PREVPREVPREV }
								}
							}
							NOT = {
								any_playable_ruler = {
									has_character_flag = rebel_attacker
									has_claim = PREV
								}
							}
						}
						random_playable_ruler = {
							limit = {
								has_character_flag = rebel_attacker
								any_demesne_title = {
									any_direct_de_jure_vassal_title = { title = PREVPREVPREV }
								}
							}
							usurp_title_plus_barony_if_unlanded = PREV
						}
					}
					if = {
						limit = {
							any_playable_ruler = {
								has_character_flag = rebel_attacker
								has_weak_claim = PREV
							}
							NOT = {
								any_playable_ruler = {
									has_character_flag = rebel_attacker
									has_strong_claim = PREV
								}
							}
						}
						random_playable_ruler = {
							limit = {
								has_character_flag = rebel_attacker
								has_weak_claim = PREV
							}
							usurp_title_plus_barony_if_unlanded = PREV
						}
					}
					if = {
						limit = {
							any_playable_ruler = {
								has_character_flag = rebel_attacker
								has_strong_claim = PREV
							}
						}
						random_playable_ruler = {
							limit = {
								has_character_flag = rebel_attacker
								has_strong_claim = PREV
							}
							usurp_title_plus_barony_if_unlanded = PREV
						}
					}
				}
			}

			# transfer de jure vassals to rebel lords
			any_vassal = {
				limit = {
					has_character_flag = rebel_attacker
					any_demesne_title = {
						any_de_jure_vassal = {
							NOT = { has_character_flag = rebel_attacker }
							vassal_of = PREVPREVPREV
							war = no
							is_patrician = no
						}
					}
				}
				any_demesne_title = {
					any_de_jure_vassal = {
						limit = {
							NOT = { has_character_flag = rebel_attacker }
							vassal_of = PREVPREVPREV
							war = no
							is_patrician = no
						}
						set_defacto_liege = PREVPREV
					}
				}
			}

			# when heir is either not an adult or not of the ruler's dynasty, find usurper for titles
			if = {
				limit = {
				    NOT = { current_heir = { is_adult = yes dynasty = PREV } }
				}
				custom_tooltip = { text = OVERTHROW_USURP_TITLES }
				hidden_tooltip = {
					# non-primary same-tier titles are given to rebels with claims
					any_demesne_title = {
						limit = {
							is_primary_holder_title_tier = yes
							is_primary_holder_title = no
							can_be_given_away = yes
						}
						if = {
							limit = {
								any_playable_ruler = {
									has_character_flag = rebel_attacker
									has_weak_claim = PREV
								}
								NOT = {
									any_playable_ruler = {
										has_character_flag = rebel_attacker
										has_strong_claim = PREV
									}
								}
							}
							random_playable_ruler = {
								limit = {
									has_character_flag = rebel_attacker
									has_weak_claim = PREV
								}
								usurp_title_plus_barony_if_unlanded = PREV
							}
						}
						if = {
							limit = {
								any_playable_ruler = {
									has_character_flag = rebel_attacker
									has_strong_claim = PREV
								}
							}
							random_playable_ruler = {
								limit = {
									has_character_flag = rebel_attacker
									has_strong_claim = PREV
								}
								usurp_title_plus_barony_if_unlanded = PREV
							}
						}
					}
					# abdicate to pretender most liked by the rebels if there is no rebel with a strong claim
					if = {
						limit = {
							primary_title = {
								NOT = {
									any_claimant = {
										has_character_flag = rebel_attacker
										has_strong_claim = PREV
									}
								}
							}
						}
						abdicate_to_most_liked_by = ROOT
					}
					# rebel with strong claim on the primary title usurps it instead
					if = {
						limit = {
							primary_title = {
								any_claimant = {
									has_character_flag = rebel_attacker
									has_strong_claim = PREV
								}
							}
						}
						random_playable_ruler = {
							limit = {
								has_character_flag = rebel_attacker
								any_claim = {
									holder = PREVPREV
									is_primary_holder_title = yes
									any_claimant = {
										character = PREVPREV
										has_strong_claim = PREV
									}
								}
							}
							random_claim = {
								limit = {
									holder = PREVPREV
									is_primary_holder_title = yes
								}
								usurp_title_plus_barony_if_unlanded = { target = PREV type = claim }
							}
						}
					}
				}
			}
			
			# pass on half of tyrant and dishonorable traits to the heir
			if = {
				limit = { current_heir = { dynasty = PREV is_adult = yes } }
				hidden_tooltip = { character_event = { id = Plus.306 } }
			}
			
			# abdicate if heir is adult, of dynasty
			if = {
				limit = { current_heir = { dynasty = PREV is_adult = yes } }
				current_heir = { prestige = -500 }
				abdicate = yes
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 250
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 250 }
		}
		# change faction moods
		FROM = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		ROOT = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		# recalculate contiguous territory province flags
		hidden_tooltip = { k_papal_state = { holder_scope = { character_event = { id = Plus.307 } } }	}		
		hidden_tooltip = { 
			any_attacker = { clr_character_flag = rebel_attacker }
			ROOT = {
				clr_character_flag = demands_lower_CA
				clr_character_flag = demands_lower_TO
				clr_character_flag = demands_tax_privilege
				clr_character_flag = demands_levy_privilege
				clr_character_flag = demands_surrender_title
				clr_character_flag = demands_lower_feudal_tax
				clr_character_flag = demands_lower_city_tax
				clr_character_flag = demands_lower_temple_tax
				clr_character_flag = demands_share_wealth
				clr_character_flag = demands_revert_succession
				clr_character_flag = demands_council_life_terms
			}
			FROM = {
				clr_character_flag = faction_feudal_war
				clr_character_flag = faction_republican_war
				clr_character_flag = faction_religious_war
				clr_character_flag = faction_court_war
			}
		}
	}

	on_fail = {
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				remove_trait = rebel
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
				}
			}
			FROM = {
				any_vassal = {
					limit = { trait = loyalist }
					remove_trait = loyalist
					reverse_remove_opinion = { who = PREV modifier = supported_civil_war }
				}
				any_vassal = {
					limit = { trait = neutral }
					remove_trait = neutral
				}
			}
		}
		ROOT = {
			prestige = -100
		}
		hidden_tooltip = {
			FROM = {
				remove_opinion = {
					who = ROOT
					modifier = declared_war
				}
			}
		}
		hidden_tooltip = {
			ROOT = {
				clr_character_flag = demands_lower_CA
				clr_character_flag = demands_lower_TO
				clr_character_flag = demands_tax_privilege
				clr_character_flag = demands_levy_privilege
				clr_character_flag = demands_surrender_title
				clr_character_flag = demands_lower_feudal_tax
				clr_character_flag = demands_lower_city_tax
				clr_character_flag = demands_lower_temple_tax
				clr_character_flag = demands_share_wealth
				clr_character_flag = demands_revert_succession
				clr_character_flag = demands_council_life_terms
			}
			FROM = {
				clr_character_flag = faction_feudal_war
				clr_character_flag = faction_republican_war
				clr_character_flag = faction_religious_war
				clr_character_flag = faction_court_war
			}
		}
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
		}
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				remove_trait = rebel
				imprison = FROM
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
					opinion = { who = PREV modifier = opinion_civil_war_loser months = 60 }
					reverse_opinion = { who = PREV modifier = opinion_coerced_into_leaving_faction months = 60 }
				}
			}		
		}	
		ROOT = {
			prestige = -200
			tooltip = { prisoner = FROM }
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 50
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 50 }
		}
		hidden_tooltip = {
			FROM = {
				any_vassal = {
					limit = { trait = loyalist }
					reverse_opinion = { who = PREV modifier = opinion_civil_war_supporter months = 60 }
					reverse_remove_opinion = { who = PREV modifier = supported_civil_war }
				}
			}
		}
		hidden_tooltip = {
			# post-civil war event to reward allies
			FROM = { character_event = { id = PlusFaction.450 } }
		}
		# change faction moods
		FROM = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		ROOT = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		hidden_tooltip = {
			ROOT = {
				clr_character_flag = demands_lower_CA
				clr_character_flag = demands_lower_TO
				clr_character_flag = demands_tax_privilege
				clr_character_flag = demands_levy_privilege
				clr_character_flag = demands_surrender_title
				clr_character_flag = demands_lower_feudal_tax
				clr_character_flag = demands_lower_city_tax
				clr_character_flag = demands_lower_temple_tax
				clr_character_flag = demands_share_wealth
				clr_character_flag = demands_revert_succession
				clr_character_flag = demands_council_life_terms
			}
			FROM = {
				clr_character_flag = faction_feudal_war
				clr_character_flag = faction_republican_war
				clr_character_flag = faction_religious_war
				clr_character_flag = faction_court_war
			}
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	attacker_ai_defeat_worth = {
		factor = 150
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	defender_ai_defeat_worth = {
		factor = 100
	}
}

religious_overthrow_ruler = {
	name = CB_NAME_OVERTHROWRULER
	war_name = WAR_NAME_FACTION_DEPOSE
	sprite = 26
	truce_days = 3650
	
	is_revolt_cb = yes
	major_revolt = yes
	can_call_allies = no
	can_ask_to_join_war = no
	
	can_use = {
		ROOT = {
			vassal_of = FROM
		}
	}

	is_valid = {
		ROOT = {
			OR = {
				liege = {
					character = PREV # either independent
				}
				liege = { 
					FROM = { 
						is_liege_or_above = PREV # or have shared liege
					}
				}
			}
		}
		FROM = {
			any_demesne_title = {
				NOT = { lower_tier_than = ROOT }
				temporary = no
			}
			OR = {
				is_parent_religion = ROOT
				is_heresy_of = ROOT
			}
			NOT = { religion = ROOT }
		}
	}

	on_success = {
		hidden_tooltip = { 
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_character_flag = rebel_attacker
				opinion = { modifier = overthrown_ruler who = FROM }
				reverse_opinion = { modifier = overthrew_me who = FROM }
			}
		}
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_tradition } }
				custom_tooltip = { text = TRADITIONFACTIONMOODPLUS }
				hidden_tooltip = {
					random_vassal = { limit = { leads_faction = faction_tradition } set_variable = { which = "faction_mood_increase" value = 4 } }
				}
			}
		}
		# weakly-held vassals of the defender may declare independence
		if = {
			limit = {
				FROM = { independent = yes }
			}
			hidden_tooltip = {
				FROM = {
					any_vassal = {
						character_event = { id = Plus.430 days = 3 }
					}
				}
			}
		}
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				remove_trait = rebel
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
				}
			}
		}
		FROM = {
			prestige = -500
			hidden_tooltip = {
				remove_opinion = { who = ROOT modifier = declared_war }
				any_vassal = {
					limit = { trait = loyalist }
					remove_trait = loyalist
					reverse_remove_opinion = { who = PREV modifier = supported_civil_war }
				}
				any_vassal = {
					limit = { trait = neutral }
					remove_trait = neutral
				}
			}
			any_demesne_title = { # Decentralize all titles
				limit = {
					OR = {
						tier = king
						tier = emperor
					}
					OR = {
						has_law = crown_authority_1
						has_law = crown_authority_2
						has_law = crown_authority_3
						has_law = crown_authority_4
					}
				}
				if = {
					limit = { has_law = crown_authority_1 }
					add_law = crown_authority_0
				}
				if = {
					limit = { has_law = crown_authority_2 }
					add_law = crown_authority_1
				}
				if = {
					limit = { has_law = crown_authority_3 }
					add_law = crown_authority_2
				}
				if = {
					limit = { has_law = crown_authority_4 }
					add_law = crown_authority_3
				}
			}
			any_demesne_title = { # Decentralize all titles
				limit = {
					OR = {
						has_law = tribal_organization_1
						has_law = tribal_organization_2
						has_law = tribal_organization_3
						has_law = tribal_organization_4
					}
				}
				if = {
					limit = { has_law = tribal_organization_1 }
					add_law = tribal_organization_0
				}
				if = {
					limit = { has_law = tribal_organization_2 }
					add_law = tribal_organization_1
				}
				if = {
					limit = { has_law = tribal_organization_3 }
					add_law = tribal_organization_2
				}
				if = {
					limit = { has_law = tribal_organization_4 }
					add_law = tribal_organization_3
				}
			}

			# find a usurper if heir is not of the attacker's religion
			if = {
				limit = { NOT = { current_heir = { religion = ROOT } } }
				custom_tooltip = { text = ABDICATE_TO_ROOT_RELIGION }
				hidden_tooltip = {
					#first abdicate to attacker who is a pretender
					if = {
						limit = {
							NOT = { has_global_flag = found_usurper }
							any_realm_character = {
								has_character_flag = rebel_attacker
								religion = ROOT
								any_pretender_title = {
									holder = PREVPREV
									is_primary_holder_title = yes
								}
							}
						}
						random_realm_character = {
							limit = {
								has_character_flag = rebel_attacker
								religion = ROOT
								any_pretender_title = {
									holder = PREVPREV
									is_primary_holder_title = yes
								}
							}
							save_event_target_as = religious_usurper
						}
						set_global_flag = found_usurper
					}
					#then abdicate to any pretender of ROOT's religion
					if = {
						limit = {
							NOT = { has_global_flag = found_usurper }
							any_realm_character = {
								religion = ROOT
								any_pretender_title = {
									holder = PREVPREV
									is_primary_holder_title = yes
								}
							}
						}
						random_realm_character = {
							limit = {
								religion = ROOT
								any_pretender_title = {
									holder = PREVPREV
									is_primary_holder_title = yes
								}
							}
							save_event_target_as = religious_usurper
							set_global_flag = found_usurper
						}
						random_independent_ruler  = {
							limit = {
								NOT = { has_global_flag = found_usurper }
								religion = ROOT
								any_pretender_title = {
									holder = PREVPREV
									is_primary_holder_title = yes
								}
							}
							save_event_target_as = religious_usurper
							set_global_flag = found_usurper
						}
						random_independent_ruler = {
							limit = {
								NOT = { has_global_flag = found_usurper }
								any_realm_character = {
									religion = ROOT
									any_pretender_title = {
										holder = PREVPREVPREV
										is_primary_holder_title = yes
									}
								}
							}
							random_realm_character = {
								limit = {
									religion = ROOT
									any_pretender_title = {
										holder = PREVPREVPREV
										is_primary_holder_title = yes
									}
								}
								save_event_target_as = religious_usurper
								set_global_flag = found_usurper
							}
						}
					}
					#then abdicate to attacker who is a strong claimant
					if = {
						limit = {
							NOT = { has_global_flag = found_usurper }
							any_realm_character = {
								has_character_flag = rebel_attacker
								religion = ROOT
								any_claim = {
									holder = PREVPREV
									is_primary_holder_title = yes
									any_claimant = {
										character = PREVPREV
										has_strong_claim = PREV
									}
								}
							}
						}
						random_realm_character = {
							limit = {
								has_character_flag = rebel_attacker
								religion = ROOT
								any_claim = {
									holder = PREVPREV
									is_primary_holder_title = yes
									any_claimant = {
										character = PREVPREV
										has_strong_claim = PREV
									}
								}
							}
							save_event_target_as = religious_usurper
						}
						set_global_flag = found_usurper
					}
					#then abdicate to a strong claimant of ROOT's religion
					if = {
						limit = {
							NOT = { has_global_flag = found_usurper }
							any_realm_character = {
								religion = ROOT
								reverse_opinion = { who = ROOT value = 0 }
								any_claim = {
									holder = PREVPREV
									is_primary_holder_title = yes
									any_claimant = {
										character = PREVPREV
										has_strong_claim = PREV
									}
								}
							}
						}
						random_realm_character = {
							limit = {
								religion = ROOT
								reverse_opinion = { who = ROOT value = 0 }
								any_claim = {
									holder = PREVPREV
									is_primary_holder_title = yes
									any_claimant = {
										character = PREVPREV
										has_strong_claim = PREV
									}
								}
							}
							save_event_target_as = religious_usurper
							set_global_flag = found_usurper
						}
						random_independent_ruler  = {
							limit = {
								NOT = { has_global_flag = found_usurper }
								religion = ROOT
								reverse_opinion = { who = ROOT value = 0 }
								any_claim = {
									holder = PREVPREV
									is_primary_holder_title = yes
									any_claimant = {
										character = PREVPREV
										has_strong_claim = PREV
									}
								}
							}
							save_event_target_as = religious_usurper
							set_global_flag = found_usurper
						}
						random_independent_ruler = {
							limit = {
								NOT = { has_global_flag = found_usurper }
								any_realm_character = {
									religion = ROOT
									reverse_opinion = { who = ROOT value = 0 }
									any_claim = {
										holder = PREVPREVPREV
										is_primary_holder_title = yes
										any_claimant = {
											character = PREVPREV
											has_strong_claim = PREV
										}
									}
								}
							}
							random_realm_character = {
								limit = {
									religion = ROOT
									reverse_opinion = { who = ROOT value = 0 }
									any_claim = {
										holder = PREVPREVPREV
										is_primary_holder_title = yes
										any_claimant = {
											character = PREVPREV
											has_strong_claim = PREV
										}
									}
								}
								save_event_target_as = religious_usurper
								set_global_flag = found_usurper
							}
						}
					}
					#then abdicate to attacker who is a weak claimant
					if = {
						limit = {
							NOT = { has_global_flag = found_usurper }
							any_realm_character = {
								has_character_flag = rebel_attacker
								religion = ROOT
								any_claim = {
									holder = PREVPREV
									is_primary_holder_title = yes
									any_claimant = { character = PREVPREV }
								}
							}
						}
						random_realm_character = {
							limit = {
								has_character_flag = rebel_attacker
								religion = ROOT
								any_claim = {
									holder = PREVPREV
									is_primary_holder_title = yes
									any_claimant = { character = PREVPREV }
								}
							}
							save_event_target_as = religious_usurper
						}
						set_global_flag = found_usurper
					}
					#then abdicate to a weak claimant of ROOT's religion
					if = {
						limit = {
							NOT = { has_global_flag = found_usurper }
							any_realm_character = {
								religion = ROOT
								reverse_opinion = { who = ROOT value = 0 }
								any_claim = {
									holder = PREVPREV
									is_primary_holder_title = yes
									any_claimant = { character = PREVPREV }
								}
							}
						}
						random_realm_character = {
							limit = {
								religion = ROOT
								reverse_opinion = { who = ROOT value = 0 }
								any_claim = {
									holder = PREVPREV
									is_primary_holder_title = yes
									any_claimant = { character = PREVPREV }
								}
							}
							save_event_target_as = religious_usurper
						}
						set_global_flag = found_usurper
					}
					#then abdicate to the primary attacker
					if = {
						limit = { NOT = { has_global_flag = found_usurper } }
						ROOT = { save_event_target_as = religious_usurper }
					}
					any_demesne_title = {
						limit = {
							is_primary_holder_title_tier = yes
							is_primary_holder_title = no
						}
						usurp_title_plus_barony_if_unlanded = { target = event_target:religious_usurper type = revolt }
					}
					primary_title = {
						usurp_title_plus_barony_if_unlanded = { target = event_target:religious_usurper type = revolt }
						hidden_tooltip = { make_primary_title = yes }
					}
					set_defacto_liege = event_target:religious_usurper
					clr_global_flag = found_usurper
					any_attacker = {
						reverse_opinion = { who = event_target:religious_usurper modifier = opinion_very_grateful years = 20 }
					}
				}
			}			
			
			# pass on half of tyrant and dishonorable traits to the heir if of the attacker's religion
			if = {
				limit = { current_heir = { religion = ROOT } }
				hidden_tooltip = { character_event = { id = Plus.306 } }
			}
			
			# abdicate if heir is of attacker's religion
			if = {
				limit = { current_heir = { religion = ROOT } }
				current_heir = { prestige = -500 }
				abdicate = yes
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 250
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 250 }
		}
		# change faction moods
		FROM = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		ROOT = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		# recalculate contiguous territory province flags
		hidden_tooltip = { k_papal_state = { holder_scope = { character_event = { id = Plus.307 } } }	}		
		hidden_tooltip = { 
			any_attacker = { clr_character_flag = rebel_attacker }
			FROM = { clr_character_flag = faction_religious_war }
		}
	}

	on_fail = {
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				remove_trait = rebel
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
				}
			}
			FROM = {
				any_vassal = {
					limit = { trait = loyalist }
					remove_trait = loyalist
					reverse_remove_opinion = { who = PREV modifier = supported_civil_war }
				}
				any_vassal = {
					limit = { trait = neutral }
					remove_trait = neutral
				}
			}
		}
		ROOT = {
			prestige = -100
			if = {
				limit = { any_vassal = { leads_faction = faction_tradition } }
				custom_tooltip = { text = TRADITIONFACTIONMOODMINUS }
				hidden_tooltip = {
					random_vassal = { limit = { leads_faction = faction_tradition } set_variable = { which = "faction_mood_decrease" value = 2 } }
					character_event = { id = PlusFaction.200 days = 1 }
				}
			}
		}
		hidden_tooltip = {
			FROM = {
				remove_opinion = {
					who = ROOT
					modifier = declared_war
				}
			}
		}
		hidden_tooltip = {
			FROM = { clr_character_flag = faction_religious_war }
		}
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_tradition } }
				custom_tooltip = { text = TRADITIONFACTIONMOODPLUS }
				hidden_tooltip = {
					random_vassal = { limit = { leads_faction = faction_tradition } set_variable = { which = "faction_mood_increase" value = 4 } }
				}
			}
		}
		hidden_tooltip = {
			any_attacker = {
				limit = { has_opinion_modifier = { who = FROM modifier = revolting_against } }
				set_defacto_liege = FROM
				remove_trait = rebel
				imprison = FROM
				any_opinion_modifier_target = {
					limit = { reverse_has_opinion_modifier = { who = PREV modifier = revolting_against } }
					reverse_remove_opinion = { who = PREV modifier = revolting_against }
					opinion = { who = PREV modifier = opinion_civil_war_loser months = 60 }
					reverse_opinion = { who = PREV modifier = opinion_coerced_into_leaving_faction months = 60 }
				}
			}		
		}	
		ROOT = {
			prestige = -200
			tooltip = { prisoner = FROM }
			if = {
				limit = { any_vassal = { leads_faction = faction_tradition } }
				custom_tooltip = { text = TRADITIONFACTIONMOODMINUS }
				hidden_tooltip = {
					random_vassal = { limit = { leads_faction = faction_tradition } set_variable = { which = "faction_mood_decrease" value = 4 } }
				}
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 50
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 50 }
		}
		hidden_tooltip = {
			FROM = {
				any_vassal = {
					limit = { trait = loyalist }
					reverse_opinion = { who = PREV modifier = opinion_civil_war_supporter months = 60 }
					reverse_remove_opinion = { who = PREV modifier = supported_civil_war }
				}
			}
		}
		hidden_tooltip = {
			# post-civil war event to reward allies
			FROM = { character_event = { id = PlusFaction.450 } }
		}
		# change faction moods
		FROM = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		ROOT = { hidden_tooltip = { character_event = { id = PlusFaction.200 days = 1 } } }
		hidden_tooltip = {
			FROM = { clr_character_flag = faction_religious_war }
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	attacker_ai_defeat_worth = {
		factor = 150
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}
	
	defender_ai_defeat_worth = {
		factor = 100
	}
}

italian_reconquest = {
	name = CB_NAME_ITALIAN_RECONQUEST
	war_name = WAR_ITALIAN_RECONQUEST
	sprite = 9
	truce_days = 3650
	is_permanent = yes
	hostile_against_others = yes
	check_de_jure_tier = DUKE # this scans all dejure duchies for the counties which are held by or vassals(or below) of selected character. Only valid if is_permanent = yes

	can_use = {
		# the attacker is a Christian with the italian_reconquest modifier
		ROOT = {
			has_character_modifier = italian_reconquest
			religion_group = christian
		}
		# the Italian reconquest has begun and not succeeded or failed
		has_global_flag = italian_reconquest_started
		NOT = { 
			has_global_flag = italian_rec_success
			has_global_flag = italian_rec_failure
		}
	}
	
	can_use_title = {
		ROOT = {
			OR = {
				ai = no
				NOT = { has_character_modifier = do_not_disturb }
			}
		}
		ROOT = { NOT = { has_character_modifier = holy_truce_timer } }
		FROM = { NOT = { has_character_modifier = holy_truce_timer } }
		# the target is an independent non-Christian who owns a duchy in Italy
		FROM = {
			NOT = { religion_group = christian }
			independent = yes # Don't attack revolters - chances are high the war will invalidate
		}	
		OR = {
			title = d_benevento
			title = d_capua
			title = d_apulia
			title = d_salerno
			title = d_calabria
			title = d_sicily
		}
	}
	
	on_add = {
		# the Pope will automatically join the war on the attacker's side
		ROOT = {
			character_event = { id = Plus.507 }
		}
	}

	is_valid = {
		ROOT = {
			has_character_modifier = italian_reconquest
		}
		NOT = { 
			has_global_flag = italian_rec_success
			has_global_flag = italian_rec_failure
		}
	}
	
	on_attacker_leader_death = {
		# give tivoli back to the pope
		if = {
			limit = {
				b_tivoli = {
					owner = {
						character = ROOT
					}
				}
			}
			ROOT = {
				religion_head = {
					b_tivoli = {
						gain_title = PREV
					}
				}
			}
		}
		# disband army and inform everyone of the reclamation failure
		disband_event_forces = yes
		end_war = invalid
		set_global_flag = italian_rec_failure
		any_playable_ruler = { narrative_event = { id = Plus.517 } }
	}

	on_success = {
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
			if = {
				limit = { any_vassal = { leads_faction = faction_tradition } }
				custom_tooltip = { text = TRADITIONFACTIONMOODPLUS }
				hidden_tooltip = {
					random_vassal = { limit = { leads_faction = faction_tradition } set_variable = { which = "faction_mood_increase" value = 4 } }
				}
			}
		}
		FROM = {
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
			if = {
				limit = { any_vassal = { leads_faction = faction_tradition } }
				custom_tooltip = { text = TRADITIONFACTIONMOODMINUS }
				hidden_tooltip = {
					random_vassal = { limit = { leads_faction = faction_tradition } set_variable = { which = "faction_mood_decrease" value = 4 } }
				}
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_piety = 50
			participation_scaled_prestige = 100
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_piety = 50 participation_scaled_prestige = 100 }
		}
		# the first time the CB is successful the attacker gets a claim to Sicily
		if = {
			limit = {
				NOT = { has_global_flag = first_war_success }
			}
			set_global_flag = first_war_success
			ROOT = { 
				add_character_modifier = { name = "holy_truce_timer" duration = 3650 } 
				k_sicily = { add_pressed_claim = ROOT } 
			}
		}
		# so long as the attacker is not a heretic then Christianity gets the holy war RA modifier
		if = {
			limit = {
				ROOT = { is_heretic = no }
			}
			ROOT = {
				religion_authority = {
					modifier = won_holy_war
					years = 20
				}
			}
		}
		# so long as the defender is not a heretic then their religion gets the lost holy war RA modifier
		if = {
			limit = {
				FROM = { is_heretic = no }
			}
			FROM = { 
				religion_authority = {
					modifier = lost_holy_war
					years = 20
				}
			}
		}
		# remove the attacker's adventurer flag if they have it
		ROOT = {
			hidden_tooltip = {
				if = {
					limit = { has_character_flag = duchy_adventurer }
					clr_character_flag = duchy_adventurer
				}
			}
		}
	}

	on_success_title = {
		# the attacker gains the duchy and gives b_tivoli to the Pope
		hidden_tooltip = {
			if = {
				limit = { 
					has_holder = yes
					holder_scope = { 
						OR = {
							character = FROM
							is_liege_or_above = FROM
						}
					}
				}
				set_global_flag = usurp_duchy
			}
		}
		ROOT = {
			gain_settlements_under_title = {
				title = PREV
				enemy = FROM
				type = invasion
			}
		}
		hidden_tooltip = {
			if = {
				limit = { 
					has_global_flag = usurp_duchy
				}
				destroy_landed_title = yes
				usurp_title = ROOT
				clr_global_flag = usurp_duchy
			}
			if = {
				limit = {
					b_tivoli = {
						owner = {
							character = ROOT
						}
					}
				}
				ROOT = {
					religion_head = {
						b_tivoli = {
							gain_title = PREV
						}
					}
				}
			}			
		}
	}

	on_fail = {	
		FROM = {
			prestige = 100
			hidden_tooltip = {
				opinion = {
					modifier = opinion_rebel_traitor
					who = ROOT
					months = 1200
				}
			}
		}
		if = {
			limit = {
				b_tivoli = {
					holder_scope = {
						character = ROOT
					}
				}
			}
			ROOT = {
				religion_head = {
					b_tivoli = {
						gain_title = PREV
					}
				}
			}
		}		
		ROOT = {
			wealth = -500
			prestige = -100 
		}
		# narrative notification to the world that the invasion failed
		if = {
			limit = {
				NOT = { has_global_flag = first_war_success }
			}
			set_global_flag = italian_rec_failure
			any_playable_ruler = { narrative_event = { id = Plus.517 } }
		}		
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		ROOT = {
			wealth = -100
		}

		if = {
			limit = {
				b_tivoli = {
					holder_scope = {
						character = ROOT
					}
				}
			}
			ROOT = {
				religion_head = {
					b_tivoli = {
						gain_title = PREV
					}
				}
			}
		}		

		FROM = {
			prestige = 200
			wealth = 100
		}

		ROOT = {
			prestige = -200
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}
}

carolingian_reconquest = {
	name = CB_NAME_CAROLINGIAN
	war_name = WAR_NAME_CAROLINGIAN
	sprite = 17
	truce_days = 3650
	is_permanent = yes
	#is_holy_war = yes
	check_de_jure_tier = DUKE # this scans all dejure duchies for the counties which are held by or vassals(or below) of selected character. Only valid if is_permanent = yes

	can_use = {
		ROOT = {
			OR = {
				ai = no
				NOT = { has_character_modifier = do_not_disturb }
			}
		}
		ROOT = { NOT = { has_character_modifier = holy_truce_timer } }
		FROM = { NOT = { has_character_modifier = holy_truce_timer } }
		ROOT = {
			has_landed_title = k_germany
			independent = yes
			religion = catholic
			capital_scope = {
				OR = {
					empire = { title = e_hre }
					kingdom = { title = k_germany }
					kingdom = { title = k_lotharingia }
					kingdom = { title = k_italy }
					kingdom = { title = k_burgundy }
					kingdom = { title = k_frisia }
				}
			}
			NOT = { is_title_active = e_karling }
			NOT = { any_realm_title = { tier = count count = 100 } }
			NOT = { has_character_flag = former_carolingian_emperor }
			NOT = { has_global_flag = hre_formed }
			NOT = { same_realm = FROM }
		}
		FROM = {
			religion_group = ROOT 
			NOT = {
				is_heresy_of = ROOT
				is_parent_religion = ROOT
			}
		}
		OR = {
			AND = {
				OR = {
					religion_group = muslim
					AND = {
						religion_group = pagan_group
						is_reformed_religion = yes
					}
				}
				religion = FROM
			}
			NOT = {
				religion_group = muslim
				religion_group = pagan_group
			}
		}
	}

	can_use_title = {
		de_jure_liege_or_above = e_hre
	}

	is_valid = {
		ROOT = {
			independent = yes
			religion = catholic
			NOT = { same_realm = FROM }
		}
	}

	on_success = {
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 100
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = {
				participation_scaled_prestige = 100
			}
		}
		FROM = {
			prestige = -100
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
	}

	on_success_title = {
		custom_tooltip = {
			text = tribal_invasion_succ_tip
			hidden_tooltip = {
				ROOT = {
					vassalize_or_take_under_title = {
						title = PREV
						enemy = FROM
						type = invasion
					}
				}
			}
		}
		if = {
			limit = {
				holder_scope = { character = FROM }
			}
			usurp_title_only = { target = ROOT type = invasion }
		}
	}

	on_fail = {
		ROOT = { prestige = -150 }
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		ROOT = {
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
			prestige = -200
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_piety = 100
			participation_scaled_prestige = 200
			dynasty_head = {
				if = {
					limit = { religion_group = muslim }
					participation_scaled_decadence = -10
				}
			}
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = {
				participation_scaled_piety = 100
				participation_scaled_prestige = 200
				dynasty_head = {
					if = {
						limit = { religion_group = muslim }
						participation_scaled_decadence = -10
					}
				}
			}
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}
}

byzantine_vassalize = {
	name = CB_NAME_BYZANTINE_CONQUEST
	war_name = WAR_NAME_VASSALIZE
	sprite = 17
	truce_days = 3650
	is_permanent = yes
	#is_holy_war = yes
	can_ask_to_join_war = yes

	can_use = {
		ROOT = {
			OR = {
				ai = no
				NOT = { has_character_modifier = do_not_disturb }
			}
		}
		ROOT = { NOT = { has_character_modifier = holy_truce_timer } }  
		ROOT = { NOT = { has_character_modifier = holy_war_timer } }
		FROM = { NOT = { has_character_modifier = holy_truce_timer } }
		# target is a neighbor or close
		FROM = {
			OR = {
				any_realm_province = {
					any_neighbor_province = {
						owner = {
							OR = {
								same_realm = ROOT
								character = ROOT
							}
						}
					}
				}
				any_realm_province = {
					ROOT = { 
						any_realm_province = {
							NOT = {
								distance = { 
									where = PREVPREV
									value = 100
								}
							}
						}
					}
				}
			}
		}
		# target is mutually disliked or the attacker is a player
		FROM = {
			OR = {
				ROOT = { ai = no }
				AND = {
					opinion = {
						who = ROOT
						value = -10
					}
					reverse_opinion = {
						who = ROOT
						value = -10
					}
				}
			}
		}
		# attacker is a strong byzantine emperor with high CA
		ROOT = {
			independent = yes
			has_landed_title = e_byzantium
			trait = strong_basileus
			OR = {
				has_law = crown_authority_3
				has_law = crown_authority_4
			}
		}
		# target is an independent count+ of the same religion group
		FROM = {
			independent = yes
			higher_tier_than = BARON
			lower_tier_than = ROOT
			religion_group = ROOT
			capital_scope = {
				county = {
					OR = {
						kingdom = { title = k_byzantium }
						kingdom = { title = k_nikaea }
						kingdom = { title = k_armenia }
						kingdom = { title = k_bulgaria }
						kingdom = { title = k_serbia }
						kingdom = { title = k_croatia }
						kingdom = { title = k_trebizond }
						kingdom = { title = k_cyprus }
						kingdom = { title = k_thessalonika }
						kingdom = { title = k_epirus }
						kingdom = { title = k_sicily }
						kingdom = { title = k_syria }
						kingdom = { title = k_jerusalem }
					}
				}
			}
		}
	}

	is_valid = {
		FROM = {
			independent = yes
			higher_tier_than = BARON
			lower_tier_than = ROOT
			religion_group = ROOT     
		}
		ROOT = {
			independent = yes
			has_landed_title = e_byzantium
			trait = strong_basileus
			OR = {
				has_law = crown_authority_3
				has_law = crown_authority_4
			}
		}      
	}
  
	on_add = {
		if = {
			limit = {
				has_global_flag = hardcore_mode
				ai = no
			}
			hidden_tooltip = { ROOT = { add_character_modifier = { name = "holy_war_timer" duration = 3650 } } }
		}
		if = {
			limit = {
				OR = {
					NOT = { has_global_flag = hardcore_mode }
					ai = yes
				}
			}
			hidden_tooltip = { ROOT = { add_character_modifier = { name = "holy_war_timer" duration = 1825 } } }
		}
	}

	on_success = {
		496 = { change_variable = { which = "imperial_decadence" value = -1 } }
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 100
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
		FROM = {
			hidden_tooltip = {
				opinion = {
					who = ROOT
					modifier = force_vassalized
					months = 240
				}
			}
			set_defacto_liege = ROOT
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
	}

	on_fail = {
		ROOT = {
			prestige = -100
		}
	}

	on_reverse_demand = {
		496 = { change_variable = { which = "imperial_decadence" value = 1 } }
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		ROOT = {
			prestige = -200
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 100
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}
}

carolingian_manifest_destiny = {
	name = CB_NAME_INVASION
	war_name = WAR_NAME_INVASION
	sprite = 8
	truce_days = 3650
	hostile_against_others = yes
	is_permanent = yes
	#is_holy_war = yes
	check_de_jure_tier = KING # this scans all dejure duchies for the counties which are held by or vassals(or below) of selected character. Only valid if is_permanent = yes

	can_use = {
		ROOT = {
			OR = {
				ai = no
				NOT = { has_character_modifier = do_not_disturb }
			}
		}
		ROOT = { NOT = { has_character_modifier = holy_truce_timer } }
		FROM = { NOT = { has_character_modifier = holy_truce_timer } }
		ROOT = {
			dynasty = 25061
			independent = yes
			religion = catholic
			higher_real_tier_than = DUKE
			NOT = { has_global_flag = hre_formed }
			NOT = { same_realm = FROM }
		}
		FROM = {
			NOT = { dynasty = ROOT }
			religion = ROOT
		}
	}

	can_use_title = {
		OR = {
			AND = {
				NOT = { has_global_flag = carolingian_empire_formed }
				kingdom = {
					OR = {
						title = k_france
						title = k_lotharingia
						title = k_bavaria
						title = k_saxony
					}
				}
			}
			AND = {
				ROOT = { has_character_flag = obj_become_exalted }
				kingdom = {
					OR = {
						title = k_france
						title = k_lotharingia
						title = k_bavaria
						title = k_saxony
						title = k_germany
						title = k_italy
						title = k_frisia
						title = k_burgundy
						title = k_aquitaine
					}
				}
			}
		}
		
		# Target must have at least 3 counties within the target kingdom
		FROM = {
			num_of_realm_counties = {
				value = 3
				title = PREV
			}
		}
		
		# The attacker needs at least one county in the target kingdom, or a border
		any_direct_de_jure_vassal_title = {
			any_direct_de_jure_vassal_title = {
				OR = {
					holder_scope = {
						OR = {
							character = ROOT
							is_liege_or_above = ROOT
						}
					}
					location = {
						any_neighbor_province = {
							owner = {
								OR = {
									character = ROOT
									is_liege_or_above = ROOT
								}
							}
						}
					}
				}
			}
		}
	}

	is_valid = {
		ROOT = {
			dynasty = 25061
			independent = yes
			religion = catholic
			NOT = { same_realm = FROM }
		}
	}
	
	is_valid_title = {
		FROM = {
			any_realm_title = {
				de_jure_liege_or_above = PREVPREV
			}
		}
	}

	on_success = {
		ROOT = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 100
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = {
				participation_scaled_prestige = 100
			}
		}
		FROM = {
			prestige = -100
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
	}

	on_success_title = {
		custom_tooltip = {
			text = tribal_invasion_succ_tip
			hidden_tooltip = {
				ROOT = {
					vassalize_or_take_under_title = {
						title = PREV
						enemy = FROM
						same_religion = yes
						type = invasion
					}
				}
			}
		}
		if = {
			limit = {
				holder_scope = { character = FROM }
			}
			usurp_title_only = { target = ROOT type = invasion }
		}
	}

	on_fail = {
		ROOT = { prestige = -150 }
	}

	on_reverse_demand = {
		FROM = {
			add_character_modifier = { name = victory_timer duration = 3 hidden = yes }
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODPLUS }
				hidden_tooltip = { character_event = { id = PlusFaction.460 days = 1 } }
			}
		}
		ROOT = {
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
			prestige = -200
			if = {
				limit = { any_vassal = { leads_faction = faction_glory } }
				custom_tooltip = { text = GLORYFACTIONMOODMINUS }
				hidden_tooltip = { character_event = { id = PlusFaction.461 days = 1 } }
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_piety = 100
			participation_scaled_prestige = 200
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = {
				participation_scaled_piety = 100
				participation_scaled_prestige = 200
			}
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}
}

